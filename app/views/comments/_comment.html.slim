=comment.body
=link_to 'delete', comment_path(comment), remote: true, method: :delete if can?(:destroy,comment)
=link_to 'edit', edit_comment_path(comment), remote: true if can?(:update,comment)
div=comment.user.name if comment.user
span
  =>time_ago_in_words(comment.updated_at) if comment.updated_at
  | ago
