=comment.body
=link_to 'delete', comment_path(comment), remote: true, method: :delete if comment.user == current_user
=link_to 'edit', edit_comment_path(comment), remote: true if comment.user == current_user
div=comment.user.name if comment.user
span
  =>time_ago_in_words(comment.updated_at) if comment.updated_at
  | ago
